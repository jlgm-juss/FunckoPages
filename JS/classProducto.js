export default class Producto {
  constructor(codigo, nombre, descripcion, imagen, tipo) {
    this.codigo = codigo;
    this.nombre = nombre;
    this.descripcion = descripcion;
    this.imagen = imagen;
    this.tipo = tipo;
  }
}
